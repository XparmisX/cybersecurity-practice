# Objective: Assessing file upload security by injecting XSS payloads via uploaded files to verify sanitization and validation filters.

in this lab, i uploaded an HTML file (xss_attack.html) with this XSS payload:

‍‍`<script>alert("upload file xss")</script>`

as a profile picture.

pwt -> where are we at (print working directory)
cd -> (change directory)            cd .. -> ?
ls -> everything that is in the folder
یه سری چیزا درباره / و اینا گفت که مثلا برگردیم یا بریم به مسیر خاصی و اینا... یا مثلا یه همچین چیزایی cd ~/Music/ 
tap and double tap are useful to find names and stuffs
mkdir -> make directory (make new files or folders?! i didn't understand) for example mkdir parmis , ls -> parmis is a new folder (empty yet)
rmdir -> remove directory  for example : rmdir parmis/           ls -> it is gone
ls -la -> looking for hidden folders
نمیفهمم این چیه : ┌──(kali㉿kali)-[~]
└─$ echo "Hi!" > test.txt
dquote> Hie!
dquote> Hi
dquote> 

cp -> copy a file  for example : cp test.txt Downloads/   ls Downloads -> test.txt is actually in there (i'm confused about these / or . stuffs)   rm Downloads/test.txt -> removed
mv -> move it  for example : mv test.txt Downloads/  ls Downloads/ -> test.txt is there        rm Downloads/test.txt
locate -> if we wanna locate a file  for example : locate bash -> it shows everything with bash in it
updatedb -> it updates everything for you, make sure using it frequently (i did not understand the relation betweeen locate and updatedb cuz it said after updatedb we can use locate again like locate bash that we tried before. idk what will actually change
why is that?! : ┌──(kali㉿kali)-[~]
└─$ updatedb
/var/lib/plocate/: Permission denied
passwd -> change the password from the default password for security
man -> man pages are my instructions for any command that i am running, most commands come with a man page. for example we wanna look at ls, we can say man ls -> giving all information about ls
also i can use ls ..help (forget it, it doesn't work)
